const serachForm = document.querySelector('form');
const serachResultDiv = document.querySelector('.search-result-item');
const container= document.querySelector('.container');

let searchQuery = '';

serachForm.addEventListener('submit',(e)  =>{
    e.preventDefault();
})